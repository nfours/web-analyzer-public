<!DOCTYPE html>
<html>
<head>
    <title>Web Page Analyzer</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        input[type=text] { width: 400px; padding: 8px; }
        input[type=submit] { padding: 8px 16px; }
        table { border-collapse: collapse; margin-top: 20px; }
        td, th { border: 1px solid #ccc; padding: 8px; }
    </style>
</head>
<body>
    <h1>Web Page Analyzer</h1>
    <form method="POST" action="/analyze">
        <input type="text" name="url" placeholder="Enter website URL" required />
        <input type="submit" value="Analyze" />
    </form>

    {{if .PageTitle}}
    <h2>Analysis Result for: {{.URL}}</h2>
    <table>
        <tr><th>HTML Version</th><td>{{.HTMLVersion}}</td></tr>
        <tr><th>Page Title</th><td>{{.PageTitle}}</td></tr>
        <tr><th>Headings</th><td>
            {{range $key, $value := .Headings}}
                {{$key}}: {{$value}}<br/>
            {{end}}
        </td></tr>
        <tr><th>Internal Links</th><td>{{.InternalLinks}}</td></tr>
        <tr><th>External Links</th><td>{{.ExternalLinks}}</td></tr>
        <tr><th>Inaccessible Links</th><td>{{.InaccessibleLinks}}</td></tr>
        <tr><th>Login Form Exists</th><td>{{.LoginFormExists}}</td></tr>
    </table>
    {{end}}
</body>
</html>
